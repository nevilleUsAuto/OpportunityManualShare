({
    doInit : function (cmp, event, helper) {
        helper.doInit(cmp, event, helper);
    },
    handleShare : function(cmp, event, helper) {
        debugger;
        alert('Handle Share: ' );
    }
})
